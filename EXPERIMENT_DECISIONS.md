# Experiment decisions

## Protocol precedence
Decision: Use the requested sequential G2 conditional dependence → G3 information ladder → G4 toy hash → G5 candidate efficacy → G6 full experiment gate namespace. Preserve RESEARCH_PLAN.md and all prior G0/G1 results unchanged.
Reason: RESEARCH_PLAN.md has older G0–G4 numbering; the latest user request and DIFFUSION_GATE_REPORT.md define this continuation.

## G2 causal evaluation and seeds
Decision: Reuse each representation's G1-C checkpoint for model_seed=0. Train only G1-C-equivalent correct-conditioned models for seeds 1/2 on the SAME saved 64/16/16 message split. Evaluate train/validation/test with sampling seeds 0/1/2 and correct/deranged/zero conditions. Save each split/seed before proceeding. No retraining per intervention. Optional random diagnostic omitted.
Reason: Isolate condition intervention from training and dataset randomness. Reinitializing the sampling generator gives identical x_T; record and assert first model input equality.
Alternative rejected: Separate training for negative controls, or rerunning G1 ladders.
Seed namespace: dataset_seed=0, split_seed=0; model_seed=0/1/2. Original seed0 shares a batch/timestep/noise generator (legacy checkpoint); new runs separate these streams. Sampling and condition streams are independent.

## G2 decision frozen before evaluation
Decision: PASS requires all three representations and model seeds 0/1/2: correct exact >=0.99 on train/validation/test, strictly greater exact than deranged and zero on every split, and nonzero output bit disagreement versus both controls. Deranged donors must have no fixed points; pairs must have identical initial noise. Missing/failed integrity → INVALID; missing model coverage → PARTIAL; valid completed metric criterion failure → FAIL. Do not proceed on any non-PASS outcome.
Reason: Matches G1 near-perfect reconstruction and user's directional causal criteria; no test-driven threshold changes. Sampling seeds are repeated measurements, not independent targets.

## Later gates
Decision: Freeze each gate protocol before its first test evaluation, use K=[1,10,100] from RESEARCH_PLAN.md for G5/G6. Do not implement or execute a later gate before the preceding gate passes. Failure before candidate efficacy implies the overall hash advantage question remains INCONCLUSIVE, not NO.

## G3 freeze before evaluation
Decision: {'gate': 'G3', 'levels': [1, 0.75, 0.5, 0.25, 0.125, 0], 'representations': ['bits', 'bgv', 'cgge'], 'model_seeds': [0, 1, 2], 'sampling_seeds': [0, 1, 2], 'mask_seed': 3100, 'mask': 'nested fixed permutation of 31 payload byte/glyph cells; floor(31*f) cells; same mask for every target; save effective fraction', 'metadata': 'At partial levels remove ALL header/validity/reserved inputs; at L0 entire condition is zero. At 100% exact original condition.', 'primary_partial_level': 0.5, 'criterion': 'all representations/seeds: full exact >=.99; partial payload bit accuracy > L0; partial payload MSE < L0; partial output differs from L0; no side inputs/leakage', 'scientific_scope': 'Partial reconstruction, including revealed content; does not test recovery of hidden bits beyond source prior or hash efficacy.', 'evaluation_split': 'same 16 unseen G1 messages; full model inputs are x_T, timestep and masked condition only'}
Reason: Whole byte/glyph cells preserve native representation. Nominal percentages are rounded down to cells and reported with their effective fractions. Fixed masks independent of target, length and split avoid adaptive position leakage. Payload-only evaluation excludes padding and metadata from the primary diagnostic. Revealed payload may imply length constraints; no separate length/header/mask input is retained. No retraining or test-driven threshold selection.

## G4 freeze before evaluation
Decision: {'experiment': {'representation': 'bits', 'source': 'printable', 'algorithm': 'md5', 'q': 8, 'dataset_size': 256, 'data_seed': 4000, 'split_seed': 4001, 'model_seed': 0, 'k': 100, 'method': 'diffusion', 'condition_mode': 'hash', 'length_conditioning': False, 'training_steps': 1500, 'batch_size': 16, 'learning_rate': 0.001, 'diffusion_steps': 50, 'sampling_steps': 50, 'beta_end': 0.4, 'prediction_type': 'sample', 'width': 32, 'condition_dim': 8, 'test_limit': None, 'device': 'cpu'}, 'alphabet': 'ABCD', 'length': 4, 'domain_size': 256, 'model_seeds': [0], 'sampling_seed': 4100, 'baseline_seed': 4200, 'bootstrap_seed': 4300, 'split_seed': 4001, 'K': [1, 10, 100], 'criterion': 'Independent oracle/evaluator agreement for every domain candidate/target prefix; no train/test message or digest overlap; correct prefix ordering; generated prefix match equals in-domain oracle membership; at least one valid held-out preimage at K=100. Zero preimages => FAIL, without config changes.', 'smoke': 'Train seed0 once, evaluate first three validation targets with K=10; integrity only, no efficacy adaptation.', 'validity': 'Codec valid AND exactly four bytes from ABCD; invalid and duplicates consume one attempt. No repair.', 'condition': '8 digest bits only; no message, length, target index or oracle lookup in generator. Dense existing hash BitDenoiser, width32 (internal128).', 'distribution': 'Uniform over 256 enumerated messages, digest-group split; toy only; does not replace main Printable94 distribution.'}
Reason: Tractable exhaustive toy domain compatible with existing four-byte-minimum codecs; q=8 follows the research-plan sanity setting. Independent reference uses hashlib.md5 plus binary-string prefix extraction, separate from production integer-shift extractor. G4 verifies pipeline operation and a nonzero valid preimage, not baseline advantage.
