Current gate: G2
Current stage: VALIDATE paired unit
Status: RUNNING

Next exact action:
```bash
.venv/bin/python -m diffusion_hash_inv.conditional_dependence --representation cgge --model-seed 0
```

Prerequisites: read EXPERIMENT_STATE.json and verify artifacts. Completed run units are validated and skipped. Do not overwrite G0/G1 or proceed past a non-PASS gate.
